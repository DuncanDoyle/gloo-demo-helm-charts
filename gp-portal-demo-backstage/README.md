# Gloo Platform Portal Backstage

## Running the chart:

Install using the following Helm command.

```
$ helm install gp-portal-demo-backstage .
```


To uninstall, simply run:
```
$ helm uninstall gp-portal-demo-backstage .
```
